# 🌿 Git 협업 가이드 (일광전구 팀프로젝트)

> 이 문서는 **git 협업을 원활히 하기 위한 "최소한의 안전장치"** 입니다.
> 복잡한 지식이 아니라 **"이대로만 하면 안 꼬인다"** 는 기본만 담았습니다.
> ⚠️ 실제로 이걸 안 지켜서 **작업이 통째로 날아간 사례**가 있었습니다.
> (로컬 main에서 커밋 없이 계속 작업 → pull 34개 밀림 → 꼬여서 복구 불가)

---

## 🚨 0. 정말 모르겠다면 — 이대로만! (급할 때 / 사고 안 나는 공식)

> 아무것도 모르겠고 급하면, **터미널(⚠️ PowerShell 말고 Git Bash / cmd)** 에
> 아래 4줄을 **순서대로** 입력하세요. 이 순서는 **절대 사고가 안 납니다.**

**① `git checkout main`**
→ 메인(main)으로 이동

**② `git pull origin main`**
→ GitHub(클라우드)의 최신 메인 코드를 받아옴 = **내 로컬 파일 최신화**

**③ `npm install`  ⭐⭐⭐ 절대 잊지 말기!**
→ 새로 추가된 라이브러리(인터랙션 등) 다운로드
→ ⚠️ **이걸 정말 자주 까먹습니다.** 안 하면 **로컬 서버로 켜봐도 오류 나는 경우가 다수 발생!** 꼭 치세요.

**④ `git checkout -b 브랜치이름`**
→ 이제 내가 작업할 **새 브랜치 생성** (예: `git checkout -b feature/내작업`)

→ 여기까지 하면 **"최신 상태 + 안전한 내 작업 공간"** 준비 끝. 이제 마음 놓고 작업하면 됩니다.
> (각 단계 자세한 설명·VS Code 버튼 방법은 아래 1번부터 참고)

---

## 📌 시작하기 전 — 커밋·올리는 방법은 2가지

같은 일을 하는 **두 가지 방법**이 있습니다. **편한 걸로** 쓰면 됩니다.

| | 방법 A — VS Code 화면(버튼) | 방법 B — 터미널 입력 |
|--|---------------------------|---------------------|
| 어떻게 | 왼쪽 **소스 제어** 패널에서 **클릭** | 명령어를 **직접 타이핑** |
| 누가 주로 | 에셋·이미지 받아서 올리는 분들 | 명령어 익숙한 분들 |
| 결과 | **둘 다 똑같음** | **둘 다 똑같음** |

> ⚠️ **터미널을 쓸 거면 PowerShell 말고 → "Git Bash" 또는 "cmd(명령 프롬프트)"** 사용.
> (PowerShell은 git에서 오류·이상동작이 잦음)
> VS Code 터미널은: 터미널 우측 `∨` → Select Default Profile → **Git Bash** 로 바꿔두면 편함.

---

## 🟢 1. 작업 시작 전 — "기본 전제" (무조건 먼저!)

**"최신 main 받기 → 새 브랜치 만들기"** 를 작업 전에 항상 합니다.

### 방법 A — VS Code 화면으로
1. **왼쪽 아래 브랜치 이름**(예: `main`) 클릭 → 목록에서 **main** 선택
2. 소스 제어 패널 위쪽 **`···` → 풀(Pull)** (또는 아래쪽 **동기화 ↻** 아이콘) → 최신 받기
3. 새 브랜치: **`···` → 분기(Branch) → 분기 만들기** → 이름 입력 (예: `feature/내작업`)
4. 터미널 열어서 **`npm install`** 한 번 (새 패키지 있을 수 있음)

### 방법 B — 터미널로 (한 줄씩 입력)
> 터미널에 입력:
- `git checkout main` → main 브랜치로 이동
- `git pull origin main` → 최신 받기 **★ 제일 중요**
- `npm install` → 새 패키지 설치
- `git checkout -b feature/내작업` → 새 브랜치 만들고 이동

> **왜?** main을 최신으로 맞춰놓고 **브랜치에서** 작업해야 남 작업과 안 부딪힙니다.
> pull 안 하고 작업하면 → 점점 밀리고 → 꼬여서 **작업이 날아갑니다.**

---

## 💾 2. 작업 → 커밋 → 올리기 (push)

### 방법 A — VS Code 화면으로 ⭐ (에셋·이미지 올리는 분들 방법)
1. 파일 수정 / 이미지 추가
2. 왼쪽 **소스 제어**(가지 모양 아이콘) 클릭 → **변경 내용**에 바뀐 파일들 보임
3. 위 **메시지 칸**에 내용 입력 (예: `feat: 스노우맨 이미지 추가`)
4. **✓ 커밋** 버튼 클릭
5. 아래쪽 **동기화(↻) / 푸시** 로 GitHub에 올림

> ⚠️ **확인 1:** 커밋 전에 **왼쪽 아래 브랜치가 `main`이 아닌지** 보세요.
>   화면에 `"main"에 커밋` 이라고 떠 있으면 → **위험!** 먼저 브랜치부터 바꾸세요. (1번 참고)
> ⚠️ **확인 2:** **커밋만 하면 내 PC에만 저장**됩니다. **동기화/푸시까지** 해야 팀에 올라가요.
>   ("커밋했는데 팀원이 안 보여요" = 푸시를 안 한 것)

### 방법 B — 터미널로
> 터미널에 입력:
- `git add .` → 바뀐 것 담기
- `git commit -m "feat: 내용"` → 커밋(저장 확정)
- `git push -u origin feature/내작업` → GitHub에 올리기

### 마무리 (공통)
→ GitHub 사이트에서 **PR(Pull Request)** 올리고 → 확인 후 **main에 머지**.
> ⚠️ main은 **PR로만** 바뀝니다. main에 **직접 push 금지.**

---

## 📖 3. 자주 쓰는 명령어 뜻 (터미널 쓸 때)

| 명령 | 뜻 |
|------|-----|
| `git` | 모든 git 명령의 시작 |
| `checkout` | 브랜치 **이동/전환** |
| `checkout -b 이름` | `-b` = **새 브랜치를 만들면서** 이동 (b = branch) |
| `pull` | 서버(원격)의 최신 내용 **받아오기** (= fetch + merge 한 번에) |
| `fetch` | 원격 최신을 **받아만** 둠 (합치진 않음) |
| `push` | 내 커밋을 서버로 **올리기** |
| `add` | 커밋할 변경분 **담기**(스테이징) |
| `commit` | 담은 것 **저장 확정** |
| `status` | **지금 상태 보기** (막히면 제일 먼저 이거!) |
| `branch` | 브랜치 목록 보기 (`*` = 현재 위치) |
| `stash` | 안 끝난 작업 **잠깐 치워두기** |
| `merge` | 다른 브랜치를 **내 브랜치에 합치기** (예: `merge main`) |

**옵션(`-`로 시작) 읽는 법:**
- `-b` → branch (새 브랜치)  ·  `-m` → message (커밋 메시지)
- 예) `git commit -m "메시지"` = 커밋 + 메시지를 한 번에

**자주 나오는 단어:**
- `origin` = 원격 저장소(GitHub)의 별명  ·  `main` = 기본(공용) 브랜치 이름
- `HEAD` = 지금 내가 있는 위치

---

## 🆘 4. 터미널이 "멈춘 것 같을 때" (초보가 제일 당황하는 것)

대부분 **고장 아니고**, 빠져나오는 방법만 모르는 경우입니다.

- **명령이 안 끝나고 멈춘 것 같다 → `Ctrl + C`**
  - 실행 중인 명령을 **강제 종료**. 잘못 친 것 같으면 일단 `Ctrl + C`로 빠져나오기.
- **글이 쫙 뜨고 맨 아래 `:` 만 깜빡거림** (`git log`, `git diff` 등)
  - 이건 "내용 보기" 모드 → **`q`** 누르면 빠져나옴 (q = quit).
- **갑자기 이상한 편집기 화면이 뜸** (`git commit` 에서 `-m`을 안 붙였을 때 = Vim 편집기)
  - 빠져나오기: **`Esc` → `:q` → Enter** (저장 안 하고 나가기)
  - 예방: 처음부터 **`commit -m "메시지"`** 로 `-m` 붙이면 이 화면 안 뜸.
- **`>` 만 뜨고 계속 입력을 기다림**
  - 따옴표 `"` 짝이 안 맞은 것 → `"` 하나 더 치고 Enter, 또는 `Ctrl + C`.
- **비밀번호를 치는데 화면에 아무것도 안 보임**
  - 정상입니다! **보안상 숨김**이에요. 그냥 치고 Enter. (키보드 고장 아님)
- **한글이 깨져 보임** → **Git Bash** 사용 권장 (cmd보다 한글이 잘 나옴).

---

## 🆘 5. pull이 막혔을 때 (제일 자주 겪는 문제)

`git pull` 했는데 이런 에러가 뜨면:
> `error: Your local changes ... would be overwritten by merge`
> `Please commit your changes or stash them before you merge. Aborting`

→ **커밋 안 한 수정**이 pull을 막는 겁니다. 안전하게 푸는 법 —
> 터미널에 입력:
- `git stash` → 안 끝난 작업 잠깐 보관
- `git pull origin main` → 최신 받기
- `git stash pop` → 보관한 작업 복원 (충돌나면 그 파일만 정리)

> 그래도 꼬이면 **혼자 `--force` 같은 거 쓰지 말고 팀에 물어보세요.**
> (잘못 건드리면 작업이 진짜 날아갑니다.)

---

## 🔄 6. 작업 중 main이 바뀌었을 때 (내 브랜치 최신화)

> 다른 사람이 main에 머지하면 **내 작업 브랜치엔 자동 반영 안 됩니다.** (main이랑 내 브랜치는 분리된 가지)
> ⚠️ **`git pull`로 main만 받는 건 main만 최신화** — 내 브랜치는 그대로! → **내 브랜치에 main을 "합쳐와야"** 합니다.

### 방법 B — 터미널 (한 줄씩)
- `git checkout main` → main으로 이동
- `git pull origin main` → main 최신 받기
- `git checkout 내브랜치` → 내 작업 브랜치로 돌아오기
- `git merge main` → **내 브랜치에 main 합치기  ★ 이 줄이 핵심**

> 짧은 버전(내 브랜치에 있는 채로): `git fetch origin` → `git merge origin/main`

### 방법 A — VS Code 화면
1. 왼쪽아래 브랜치를 **main**으로 → 동기화(↻)로 최신 받기
2. 다시 **내 브랜치**로 전환
3. `···` → **분기(Branch) → 분기 병합(Merge Branch…)** → **main** 선택

### ⚔️ 충돌(conflict)이 뜨면
같은 파일 **같은 줄**을 main이랑 나랑 둘 다 고쳤을 때만 떠요:
```
<<<<<<< 내 코드
...
=======
...
>>>>>>> main
```
→ 그 파일 열어서 **맞는 것만 남기고** `<<<` · `===` · `>>>` 줄들 지우기 → `git add 파일` → `git commit`. 끝.
(안 겹치면 git이 알아서 합쳐서 충돌 안 남)

### ⏰ 언제 하나
- **작업 시작 전** / **작업 중 main 많이 바뀌면 가끔** / **PR 올리기 직전**
- 💡 **자주 받을수록 충돌이 작아요.** 오래 묵히면 한꺼번에 터집니다.

---

## ✅ 한 장 요약

**작업 시작**
- 최신 받기: VS Code 동기화 ↻  /  또는 `git checkout main` → `git pull origin main`
- 새 브랜치: VS Code 분기 만들기  /  또는 `git checkout -b feature/이름`
- `npm install`

**작업 중**
- VS Code: 메시지 입력 → **✓ 커밋**  /  또는 `git add .` → `git commit -m "..."`
- **자주** 커밋!
- main이 바뀌면 → `git merge main`으로 **내 브랜치 최신화** (6번 참고)

**작업 끝**
- VS Code: **동기화/푸시**  /  또는 `git push -u origin feature/이름`
- → GitHub에서 **PR**

**3대 원칙**
1. 작업 전 **최신 받기(pull) 먼저**
2. **main 말고 브랜치**에서 작업
3. **커밋·푸시 자주** (몰아서 X) — *커밋만 X, 푸시까지 해야 올라감*

> 이 가이드는 **최소한의 안전장치**입니다.
> 이것만 지켜도 "작업이 통째로 날아가는" 일은 생기지 않습니다.
